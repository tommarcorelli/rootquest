{
  "name": "rootquest",
  "version": "1.10.0",
  "private": true,
  "description": "rootQuest — browser-based Linux privilege-escalation training game (23 machines, bilingual EN/FR, vanilla JS).",
  "scripts": {
    "serve": "node tests/serve.js",
    "test:logic": "node tests/harness.js",
    "test": "playwright test",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
